# Trip Planner 需求提取优化方案基准测试

## 1. 测试目的

对比以下两种 `trip_planner` 需求提取优化方案的耗时、能力路由准确率和字段提取准确率，为后续实现方案选择提供依据。

### 方案 1.1：Schema 瘦身与两阶段/并行拆分

第一阶段使用轻量 LLM 调用，只提取全局字段和子模块开关：

```json
{
  "destination_city": "西安",
  "duration_days": 3,
  "start_date": null,
  "need_transport": true,
  "need_hotel": true
}
```

第二阶段根据开关并行调用：

- `Transport Extractor`：只提取交通槽位。
- `Hotel Extractor`：只提取酒店槽位。

交通和酒店同时启用时，两个子提取器使用异步并发执行。

### 方案 2：规则优先，歧义时才调用 LLM

执行顺序：

1. 代码快速解析明确的城市、天数、日期、交通和酒店信息。
2. 判断表达是否存在多种合理理解，而不是简单判断字段是否完整。
3. 表达明确但字段缺失时，不调用 LLM，直接进入缺失项校验。
4. 存在指代、冲突或多种解释时，只让 LLM 处理对应的模糊字段。
5. 最后由代码统一组装、派生默认值并裁决交通和酒店能力。

## 2. 测试环境与边界

- 测试日期：2026-07-26
- 模型：`qwen3.7-flash`
- 单次 LLM 超时：30 秒
- JSON 校验：Pydantic
- JSON 不合法时：最多重试一次
- 测试样例数：12 条
- 每个方案使用相同模型、相同超时和相同重试策略

本测试只测量进入 `trip_planner` 后的需求提取阶段，不包含：

- 顶层 `general_agent / trip_planner` 路由耗时
- 地图和天气查询
- 高铁、航班和酒店工具调用
- 行程文案生成
- 数据库写入和前端渲染

两个方案均使用普通 JSON Prompt 和 Pydantic 校验，以隔离测试 Schema 拆分和规则优先策略本身，不使用当前 `StructuredOutputService` 的原生结构化输出加二次回退机制。

## 3. 测试集覆盖范围

12 条样例覆盖了以下场景：

1. 城市攻略，同时查询高铁和酒店，但缺少日期、交通出发城市。
2. 只有城市、天数和开始日期的核心攻略。
3. 启用单程高铁，同时明确酒店已经订好。
4. 比较飞机和高铁，同时推荐酒店。
5. 只描述准备乘坐高铁和住宿位置，但没有要求实时查询。
6. 查询机票，同时明确酒店不用查询。
7. 城市、日期、出发地、往返航班和酒店位置都明确的复杂请求。
8. “上海或者苏州”这种多目的地歧义。
9. 只有三天攻略和高铁查询，但缺少目的地。
10. “三天，不对，改成四天”这种冲突修正。
11. 多轮对话中的城市、天数继承和新日期、出发城市补充。
12. “那就去那里吧”这种无法唯一确定的指代。

## 4. 评估指标

### 核心字段准确率

每条样例评估：

- `destination_city`
- `duration_days`
- `start_date`

共计 `12 × 3 = 36` 个判断。

### 能力路由准确率

每条样例评估：

- `need_transport`
- `need_hotel`

共计 `12 × 2 = 24` 个判断。

### 子模块槽位准确率

每条样例评估：

- `modes`
- `journey_scope`
- `origin_city`
- `nearby_poi`

共计 `12 × 4 = 48` 个判断。

### 整条请求完全正确率

只有该条样例的全部 9 个字段都与标准答案一致，才记为完全正确。

## 5. 总体测试结果

| 指标 | 方案 1.1：两阶段/并行 LLM | 方案 2：规则优先、歧义调用 LLM |
|---|---:|---:|
| 平均耗时 | 20.25 秒 | 1.21 秒 |
| 中位耗时 | 18.43 秒 | 约 0.75 毫秒 |
| 最快耗时 | 10.28 秒 | 0.1 毫秒 |
| 最慢耗时 | 33.44 秒 | 5.93 秒 |
| LLM 总调用数 | 29 次 | 3 次 |
| JSON 重试次数 | 4 次 | 0 次 |
| 调用 LLM 的样例比例 | 100% | 25% |
| 核心字段准确率 | 97.22% | 100% |
| 交通/酒店能力路由准确率 | 100% | 100% |
| 子模块槽位准确率 | 95.83% | 100% |
| 整条请求完全正确率 | 75% | 100% |
| 子提取器失败样例数 | 3 条 | 0 条 |

> 方案 2 的整体平均耗时被 3 条歧义样例的 LLM 调用拉高。其余 9 条不调用 LLM 的明确或缺字段样例基本都在毫秒级完成。

## 6. 方案 1.1 结果分析

### 6.1 性能

方案 1.1 相比当前一次性提取完整 `TripPlanningRequest` 有明显改善：

- 当前完整 Schema 提取实测平均约 35.09 秒。
- 两阶段拆分后平均约 20.25 秒。

但是，Schema 拆分没有减少远程调用次数：

```text
全局字段和能力开关提取
  ↓
交通槽位提取 ─┐
              ├─ 并行
酒店槽位提取 ─┘
```

并行只能降低墙钟时间，不能减少总请求数。12 条样例共发起 29 次 LLM 调用，平均每条约 2.42 次。

### 6.2 稳定性

本轮出现：

- 2 次交通子提取器最终失败
- 1 次酒店子提取器最终失败
- 4 次 JSON 重试

子提取器失败时，第一阶段得到的核心字段和交通/酒店开关仍然可以保留，但细粒度槽位可能丢失。

### 6.3 准确率

- 交通和酒店能力开关全部判断正确。
- 核心字段有 1 个判断错误。
- 子模块槽位有 2 个判断错误。
- 9/12 条请求全部字段完全正确。

方案 1.1 的主要问题不是第一阶段路由，而是多次 LLM 调用扩大了格式错误、超时和子模块失败的概率。

## 7. 方案 2 结果分析

### 7.1 不调用 LLM 的明确样例

12 条样例中有 9 条表达明确，或者只是缺少字段，因此没有调用 LLM。

这些样例的处理耗时约为：

- 最快：0.1 毫秒
- 大部分：0.1～1.3 毫秒
- 包含更多文本解析的样例：约 7.4 毫秒

代码会直接完成：

- 城市、天数和日期解析
- 交通和酒店能力启停判断
- 飞机、高铁、单双程识别
- 交通出发城市识别
- 酒店附近地点识别
- 多轮上下文继承
- 缺失项判断

### 7.2 调用 LLM 的歧义样例

只有以下 3 条触发了 LLM：

1. 多目的地：“上海或者苏州”
2. 数值冲突：“三天，不对，改成四天”
3. 指代歧义：“那就去那里吧”

耗时分别约为：

- 5.71 秒
- 5.93 秒
- 2.88 秒

平均约 4.84 秒，均未发生 JSON 重试。

### 7.3 准确率

在本次 12 条开发基准中：

- 核心字段准确率：100%
- 交通/酒店能力路由准确率：100%
- 子模块槽位准确率：100%
- 整条请求完全正确率：100%

## 8. 原始示例的对比结果

测试输入：

> 去西安三天旅游攻略，顺便查一下去西安的高铁，以及西安北站附近的酒店。

标准结果：

```json
{
  "destination_city": "西安",
  "duration_days": 3,
  "start_date": null,
  "need_transport": true,
  "need_hotel": true,
  "modes": ["train"],
  "journey_scope": "round_trip",
  "origin_city": null,
  "nearby_poi": "西安北站"
}
```

### 方案 1.1

- 耗时：33.35 秒
- LLM 调用：4 次
- JSON 重试：1 次
- 城市、天数、能力开关和酒店位置正确
- 交通子提取器最终失败，导致 `modes=["train"]` 丢失

### 方案 2

- 耗时：7.4 毫秒
- LLM 调用：0 次
- JSON 重试：0 次
- 全部字段正确

两个方案最终都会发现以下缺失项：

- `core.start_date`
- `transport.origin`

随后应直接追问：

> 请一次补充：出行开始日期、交通出发城市。

## 9. 测试中发现的规则设计要求

方案 2 不能只做整句话的简单关键词匹配，至少需要满足以下要求。

### 9.1 按子句判断能力启停

例如：

> 酒店已经订好了，帮我查机票。

“已经订好”只能关闭酒店能力，不能误伤机票查询。因此交通和酒店启停必须结合各自所在子句判断。

### 9.2 支持完整的天数表达

需要覆盖：

- 三天
- 玩 3 天
- 四日游
- 五日行程

### 9.3 酒店附近地点应截取最近的地点短语

例如：

> 查往返航班和春熙路附近酒店。

`nearby_poi` 应为“春熙路”，不能把前面的航班查询文本一起截取。

### 9.4 区分“明确缺失”与“存在歧义”

例如：

> 帮我做三天旅游攻略，顺便查高铁。

这句话缺少目的地和交通出发城市，但不存在多种合理理解。应直接进入缺失项校验，而不是调用 LLM。

### 9.5 不应强行消除真实歧义

例如：

> 我想去上海或者苏州玩三天。

或者：

> 那就去那里吧。

如果上下文不能唯一确定目的地，LLM 也必须返回 `null`，由系统继续向用户澄清，不能猜测。

## 10. 局限性

本测试属于小规模开发基准，不能直接视为生产准确率：

- 只有 12 条人工标注样例。
- 方案 2 的规则在相同开发样例上经过修正，尚未使用独立盲测集验证。
- 没有覆盖错别字、方言、语音转写错误和更长的历史对话。
- 城市识别原型使用有限城市词表；正式实现需要可靠的行政区/城市词典。
- LLM 输出具有随机性，方案 1.1 的失败率需要多轮重复测试才能稳定估计。
- 测试没有计算模型 token 消耗和 API 成本。
- 测试没有包含顶层路由及后续工具调用耗时。

正式实施前建议增加一组不参与规则开发的独立盲测集，并在多次重复运行后统计平均准确率、P95 延迟和失败率。

## 11. 方向性结论

当前结果明显支持以方案 2 作为主流程：

```text
规则快速解析
  ↓
确定字段、缺失字段和歧义字段
  ↓
是否存在真实歧义？
  ├─ 否 → 直接组装、校验、追问或执行
  └─ 是 → 只调用对应的小型歧义提取器
             ├─ 核心字段歧义
             ├─ 交通字段歧义
             └─ 酒店字段歧义
```

方案 2 也可以吸收方案 1.1 的子模块拆分思想：不是每次都调用全局、交通和酒店三个 LLM，而是只有某个模块出现真实歧义时，才调用该模块的小型 Schema 提取器。

基于本次开发基准：

- 方案 1.1 能把当前约 35 秒的完整 Schema 提取降低到约 20 秒，但调用次数和失败面增加。
- 方案 2 对大多数明确或仅缺字段的请求可以在毫秒级完成。
- 方案 2 的歧义请求平均约 4.84 秒。
- 方案 2 在本次样例中的准确率和稳定性均优于方案 1.1。

## 12. 工具调用结果（60 条端到端评测）

- 测试日期：2026-07-28

### 测试内容

使用 `evals/eval_case.jsonl` 中的 60 条案例，覆盖普通聊天、天气、航班、火车、酒店、完整规划、混合请求、缺少信息、多轮修改和歧义输入。每条案例预先标注预期路由、结果类型、必要/允许工具和参数字段。

评测执行真实路由、模型和工具链。前 41 条主模型为 `qwen3.7-flash-2026-07-15`，第 42～60 条主模型为 `qwen3.7-plus-2026-05-26`；路由模型均为 `qwen3.7-flash-2026-07-15`。

### 结果如何得到

通过 `scripts/run_eval.py` 调用项目的 `ChatService`，从流式事件和规划 trace 中记录实际路由、工具名称、工具参数、执行状态、Schema 状态、模型/工具调用次数和端到端耗时，再由 `app.evals.trip_eval` 与预期结果逐项比较。原始结果及报告位于 `evals/results/full-v2-mixed/`。

### 核心结果

| 指标 | 60 条混合结果 | 后 19 条 Plus 结果 |
|---|---:|---:|
| 路由准确率 | 91.67% | 84.21% |
| 必要工具召回率 | 87.72% | 88.24% |
| 多余工具调用率 | 0% | 0% |
| 参数字段准确率 | 78.80% | 73.61% |
| 工具执行成功率 | 97.71% | 100% |
| Schema 合法率 | 100% | 100% |
| 结果可用率 | 56.14% | 43.75% |
| 最终规划校验通过率 | 19.23% | 0% |
| 端到端 p50 / p95 | 14.45 秒 / 97.03 秒 | 63.99 秒 / 111.06 秒 |

后 19 条没有再出现模型额度不足，但有 9 条在结构化攻略生成阶段约 90 秒后失败；测试期间还出现高德 `10021` 限流和小红书研究服务不可用。由于 60 条结果混用了两个主模型，只适合作为当前系统基线，不应直接用于模型横向比较。