# 项目故事：让不完整的旅行方案诚实地说出“我还没准备好”

## 背景

旅游规划系统需要同时依赖航班、火车、酒店、天气、景点和地图路线等外部数据。一次规划过程中，即使工具调用返回“成功”，也不代表系统已经取得了可用于生成行程的事实。例如，供应商可能返回空列表、结果无法标准化，或者只查到了部分地点之间的路线。

过去，系统只用“成功/失败”描述规划阶段。这种二元状态无法表达“已经生成方案，但关键事实不完整”的情况。因此，一个缺少往返交通、路线耗时或完整预算的方案仍可能被当作成功结果展示；前端也会显示绿色完成标识，让用户误以为它已经可以执行。

## 用户故事

作为准备出行的用户，

我希望系统明确告诉我旅行方案是完整可执行、部分降级，还是因关键事实缺失而不可执行，

并且说明具体缺少什么、在哪个阶段发生、会产生什么影响，

以便我判断是否可以直接采用方案，还是需要重新查询或补充信息。

## 设计决策：三态就绪度

系统为旅行方案引入统一的三态就绪度：

| 状态 | 含义 | 用户看到的结果 |
| --- | --- | --- |
| `ready` | 关键事实齐全且校验通过，方案可执行 | 正常展示正式行程 |
| `partial` | 已生成方案，但部分数据或能力发生降级 | 展示方案，同时明确提示降级原因和影响 |
| `blocked` | 缺少往返交通、住宿、关键路线、预算等必要事实，无法形成可信方案 | 展示不完整草稿和阻断原因，不保存为正式行程 |

这里的状态不是根据“工具有没有报错”判断，而是根据标准化之后是否获得了足够的可用数据，以及最终方案是否通过可执行性校验来判断。

单个规划阶段仍使用执行状态 `success / partial / failed`：阶段状态描述一次处理是否完整完成，方案就绪度则回答最终行程能否执行。两者共同输出，但不会混为同一个字段。

## 结构化诊断

每个降级或阻断原因都使用结构化诊断记录，而不是只拼接一段不可检索的错误文本。诊断至少包含：

```json
{
  "code": "missing_return_transport",
  "stage": "validating_itinerary",
  "severity": "error",
  "message": "未取得可核验的返程交通方案，当前行程不可执行。",
  "details": {
    "origin": "西安",
    "destination": "南京",
    "date": "2026-07-20"
  }
}
```

字段语义如下：

- `code`：稳定、可统计的诊断代码，供测试、日志、监控和前端逻辑使用。
- `stage`：问题发生或被发现的规划阶段。
- `severity`：`info`、`warning` 或 `error`。
- `message`：面向用户的可读说明。
- `details`：相关查询条件、覆盖数量或其他可追溯上下文。

## 后端行为

后端在数据采集、行程生成和最终校验三个层面共同维护就绪度：

1. 数据采集阶段区分“供应商调用成功”和“取得可用标准化记录”，并检查往返方向及路线覆盖。
2. 降级生成器只能使用已核验的路线耗时；没有完整路线证据时，耗时保持未知，不再填入经验常量冒充事实。
3. 最终校验把缺少往返交通、过夜住宿、预算总额或活动间关键路线视为阻断问题。
4. 通过校验的方案标记为 `ready` 并保存为正式版本。
5. 存在非阻断降级的方案标记为 `partial`，同时保留诊断信息。
6. 存在关键错误的方案标记为 `blocked`，只保存请求草稿或保留原有正式版本，不让不完整方案覆盖有效行程。

诊断会随方案一起输出和持久化，使 API 调用方、日志系统与后续规划节点看到同一份事实。

## 前端行为

前端不再把所有已结束阶段都显示为绿色成功：

- `ready` 方案使用正常完成样式。
- `partial` 方案展示降级提示；处于 `partial` 的规划阶段使用琥珀色警告样式和警告图标。
- `blocked` 方案明确提示这是不可执行的草稿，列出阻断项，引导用户重新查询或补充必要信息。

最终行程正文中增加“数据质量”说明，使用户即使忽略过程状态，也能在结果中看到缺失事实及其影响。

## 示例场景

用户计划从南京前往西安旅行两天。航班和火车工具请求均正常结束，但没有返回可标准化的往返班次；地图矩阵查询触发限流，只取得一个地点对的路线。

系统不再输出一个带有虚构固定交通耗时的“成功行程”，而是：

1. 将交通采集阶段标记为 `failed`，并记录往返交通缺失诊断。
2. 将路线采集标记为 `partial`，说明只覆盖了部分地点。
3. 在校验阶段阻止该方案成为正式行程。
4. 向用户展示不完整草稿，并明确说明需要重新查询交通和路线数据。

## 验收标准

- 原始工具调用成功但标准化结果为空时，采集阶段不得标记为 `success`，方案也不得标记为 `ready`。
- 只取得部分必要数据时，系统返回 `partial`，并至少包含一条可读、可追踪的诊断。
- 缺少往返交通、过夜住宿、预算总额或活动间必要路线时，方案返回 `blocked`。
- `blocked` 方案不得覆盖现有正式版本，也不得作为新的正式行程保存。
- 未核验路线不得生成固定经验耗时。
- API 返回方案状态和结构化诊断，前端能够正确解析并区分展示三种状态。
- 用户能从最终输出中看出发生了什么、缺少什么以及方案是否可直接执行。

## 项目价值

这项改动并不是增加一组界面标签，而是重新定义系统对“成功”的判断：成功不再意味着流程运行结束，而意味着关键旅行事实充分、方案经过校验并且具备可执行性。

通过 `ready / partial / blocked` 和结构化诊断，系统能够在外部服务不稳定时诚实降级，避免把空数据、经验估算或局部结果包装成确定事实，同时为故障统计、重试策略和后续人工介入提供统一基础。

## 小红书 MCP 的 HTTP / stdio 双传输

主项目通过统一的 `XhsMcpClient` 兼容 `streamable-http` 和 `stdio`，上层规划、登录、搜索与详情读取逻辑不感知传输差异。

- `stdio`：FastAPI 首次调用 MCP 时按需启动本机子进程，复用同一会话，并在 API 关闭时回收；无需端口和 Bearer Token，适合本机 Chrome 登录及验证码场景。
- `streamable-http`：主项目通过 URL 和 Token 连接独立 MCP 服务，适合 Docker、多容器或需要独立管理 MCP 生命周期的部署方式。
- 使用 `XHS_MCP_TRANSPORT=stdio|streamable-http` 切换；HTTP 的 URL/Token 和 stdio 的命令/参数分别配置。

保留双传输的原因是登录验证通常需要本机可见浏览器，而未来容器化或独立服务部署仍更适合 HTTP；两种模式共用数据契约和错误处理，避免维护两套业务链路。
